<div class="row">
	{{#items}}
		<div class="col-xs-4 col-sm-3 col-lg-2">
			<a href="{{users_page_url}}?userid={{id}}" class="thumbnail avatar">
				<img src="{{avatar}}&s=128" alt="{{fullname}}">
				<div class="name">{{fullname}}</div>
			</a>
		</div>
	{{/items}}
</div>