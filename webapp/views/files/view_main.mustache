<div id="files_folders_container">
	{{> view_files_folders}}
</div>

{{#modals}}
	<div class="modal fade hide" id="{{id}}">
	  <div class="modal-header">
	    <button type="button" class="close" data-dismiss="modal">×</button>
	    <h3 class="action_add">{{title}}</h3>
	  </div>
	  <div class="modal-body">
	  </div>
	  <div class="modal-footer">
	    <a href="#" class="btn" data-dismiss="modal">{{app_str_system_cancel}}</a>
	    <a href="#" id="submitAddFile" class="btn {{btn_class}}" data-loading-text='{{btn_title}}...' onclick="$(this).button('loading'); $('form', '#{{id}}').submit(); return false;">{{btn_title}}</a>
	  </div>
	</div>	
{{/modals}}