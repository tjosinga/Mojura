<div id="files_folders_container">
	{{> view_files_folders}}
</div>

{{#modals}}
	<div class="modal fade" id="{{id}}">
		<div class="modal-dialog">
			<div class="modal-content">
				<div class="modal-header">
			    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
			    <h4 class="action_add modal-title">{{title}}</h4>
			  </div>
			  <div class="modal-body">
			  </div>
			  <div class="modal-footer">
			    <a href="#" class="btn" data-dismiss="modal">{{locale_str_system_cancel}}</a>
			    <a href="#" class="btn btn-default {{btn_class}}" data-loading-text='{{btn_title}}...' onclick="$(this).button('loading'); $('form', '#{{id}}').submit(); return false;">
				    <span class='fa {{icon}}'></span> {{btn_title}}
			    </a>
			  </div>
			</div>
		</div>
	</div>
{{/modals}}